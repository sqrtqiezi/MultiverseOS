{
  "name": "multiverseos",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev": "pnpm -r --parallel dev",
    "verse": "node packages/cli/dist/index.js"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
